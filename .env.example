# The browser calls the same-origin proxy to avoid HTTPS-to-HTTP mixed content.
VITE_API_BASE_URL=/api
# Development-only Vite proxy target. Production uses API_PROXY_TARGET in Portainer.
VITE_API_PROXY_TARGET=http://165.140.22.23:3000
